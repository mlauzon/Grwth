<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ProgressBar
        android:id="@+id/skillCircleProgressBar"
        android:layout_width="200dp"
        android:layout_height="200dp"
        style="@style/CircularDeterminateProgressBar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/skillCircleProgressName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"
        app:layout_constraintBottom_toBottomOf="@id/skillCircleProgressBar"
        app:layout_constraintTop_toTopOf="@id/skillCircleProgressBar"
        app:layout_constraintStart_toStartOf="@id/skillCircleProgressBar"
        app:layout_constraintEnd_toEndOf="@id/skillCircleProgressBar"
        android:hint="Skill" />
</androidx.constraintlayout.widget.ConstraintLayout>